{
  "name": "ui",
  "version": "1.0.0",
  "description": "",
  "main": "script.js",
  "scripts": {
    "build": "esbuild js/main.js --bundle --outfile=dist/bundle.js --minify",
    "watch": "esbuild js/main.js --bundle --outfile=dist/bundle.js --watch",
    "dev": "concurrently \"npm:watch\" \"cargo run --manifest-path=../../Cargo.toml\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.2.0",
    "esbuild": "^0.25.8"
  }
}
